import{Link}from"@tanstack/react-router";import{useQuery}from"@tanstack/react-query";import{AlertTriangle,ClipboardCheck,GraduationCap,Users,CalendarDays,MessageSquareWarning}from"lucide-react";import{useCurrentRole}from"@/hooks/useAuth";import{supabase}from"@/integrations/supabase/client";import{availabilityQuery,debriefsQuery,evaluationsQuery,membersQuery,programsQuery}from"@/lib/icc";
const db=()=>supabase as any;function perm(uid:string|undefined){return useQuery({queryKey:["org-dashboard-scope",uid],enabled:!!uid,queryFn:async()=>{const tries=[["programmes","consulter"],["membres","consulter"],["pilotage","consulter"]];for(const[p_module,p_action]of tries){const{data}=await db().rpc("access_scope_for_user",{p_user:uid,p_module,p_action});if(data&&data!=="interdit")return data as string}return"interdit"}})}
export function OrganizationDashboard(){const{member,userId,isTechAdmin}=useCurrentRole(),scope=perm(userId),programs=useQuery(programsQuery),members=useQuery(membersQuery),av=useQuery(availabilityQuery),debriefs=useQuery(debriefsQuery),evals=useQuery(evaluationsQuery);const training=useQuery({queryKey:["org-training"],queryFn:async()=>{const{data,error}=await db().from("member_training_paths").select("*");if(error)throw error;return data??[]}});const team=useQuery({queryKey:["org-teamlife"],queryFn:async()=>{const{data,error}=await db().from("team_life_events").select("id,title,starts_at,status,audience_kind,audience_pole_ids").in("status",["scheduled","postponed"]).is("deleted_at",null);if(error)throw error;return data??[]},retry:false});const s=isTechAdmin?"tous":scope.data;if(!s||s==="interdit"||s==="moi")return null;const myPoles=(member as any)?.pole_ids??[];const all=s==="tous"||s==="all";const inScope=(poleId:string)=>all||myPoles.includes(poleId);const scopedPrograms=(programs.data??[]).filter(p=>!p.archived&&p.status!=="cancelled"&&(all||p.assignments.some(a=>inScope(a.pole_id))));const today=new Date().toISOString().slice(0,10),up=scopedPrograms.filter(p=>(p.start_date??"9999")>=today);let needs=0,pending=0;for(const p of up)for(const a of p.assignments.filter(x=>inScope(x.pole_id))){needs+=Math.max(0,Number(a.required_count??0)-a.memberIds.length);pending+=a.memberIds.filter(id=>!p.responses.some(r=>r.member_id===id&&r.status!=="pending")).length}const scopedMemberIds=new Set((members.data?.links??[]).filter((l:any)=>all||inScope(l.pole_id)).map((l:any)=>l.member_id));const pendingAvail=(av.data??[]).filter((x:any)=>x.status==="pending"&&(all||scopedMemberIds.has(x.member_id))).length;const finished=scopedPrograms.filter(p=>p.start_date&&p.start_date<today),debriefIds=new Set((debriefs.data??[]).map((d:any)=>d.program_id)),missingDebrief=finished.filter(p=>!debriefIds.has(p.id)).length;const evalPending=(evals.data??[]).filter((e:any)=>["submitted","revision_requested"].includes(e.status)&&(all||scopedMemberIds.has(e.subject_member_id))).length;const trainingActive=(training.data??[]).filter((x:any)=>x.status!=="completed"&&(all||scopedMemberIds.has(x.member_id))).length;const teamUpcoming=(team.data??[]).filter((e:any)=>e.starts_at&&e.starts_at>=new Date().toISOString()&&(e.audience_kind==="team"||all||(e.audience_pole_ids??[]).some((id:string)=>myPoles.includes(id)))).length;const cards=[{n:needs,l:"Besoins non couverts",to:"/programmes",icon:AlertTriangle},{n:pending,l:"Confirmations attendues",to:"/programmes",icon:ClipboardCheck},{n:pendingAvail,l:"Indisponibilités à traiter",to:"/disponibilites",icon:CalendarDays},{n:missingDebrief,l:"Post-services manquants",to:"/post-service",icon:MessageSquareWarning},{n:evalPending,l:"Évaluations à traiter",to:"/evaluations",icon:Users},{n:trainingActive,l:"Personnes en formation",to:"/formations",icon:GraduationCap}];return <section className="mt-6 rounded-3xl border bg-card p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-black text-icc-violet">Vue organisation</h2><p className="text-xs text-muted-foreground">Pilotage réel de {all?"toute la COM":"mon périmètre"}. Les compteurs sont calculés depuis les données de l’application.</p></div><span className="rounded-full bg-muted px-3 py-1 text-xs font-bold">{all?"Toute la COM":"Mon / mes pôles"}</span></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{cards.map(c=><Link key={c.l} to={c.to as any} className="rounded-2xl border p-4 hover:border-icc-violet/40"><c.icon className="size-5 text-icc-violet"/><b className="mt-1 block text-2xl text-icc-violet">{c.n}</b><small className="text-muted-foreground">{c.l}</small></Link>)}</div><div className="mt-4 grid gap-3 md:grid-cols-3"><div className="rounded-2xl bg-muted/35 p-4"><b className="text-xl text-icc-violet">{up.length}</b><p className="text-xs">Programmes à venir dans le périmètre</p></div><div className="rounded-2xl bg-muted/35 p-4"><b className="text-xl text-icc-violet">{scopedMemberIds.size}</b><p className="text-xs">Membres dans le périmètre</p></div><Link to="/vie-equipe" className="rounded-2xl bg-muted/35 p-4"><b className="text-xl text-icc-violet">{teamUpcoming}</b><p className="text-xs">Communions fraternelles à venir</p></Link></div></section>}
