import { PROGRAM_FORMAT_OPTIONS,PROGRAM_IMPORTANCE_OPTIONS,PROGRAM_RECURRENCE_OPTIONS,PROGRAM_TYPE_OPTIONS } from "@/lib/programLabels";
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
export type ProgramFilterState={status:string;type:string;format:string;recurrence:string;importance:string};
export const DEFAULT_PROGRAM_FILTERS:ProgramFilterState={status:"all",type:"all",format:"all",recurrence:"all",importance:"all"};
export function ProgramFilters({value,onChange}:{value:ProgramFilterState;onChange:(v:ProgramFilterState)=>void}){return <div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-5"><Filter value={value.status} set={v=>onChange({...value,status:v})} items={[["all","Tous les statuts"],["confirmed","Confirmé"],["unconfirmed","Non confirmé"],["postponed","Reporté"],["cancelled","Annulé"]]}/><Filter value={value.type} set={v=>onChange({...value,type:v})} items={[["all","Tous les types"],...PROGRAM_TYPE_OPTIONS] as any}/><Filter value={value.format} set={v=>onChange({...value,format:v})} items={[["all","Tous les formats"],...PROGRAM_FORMAT_OPTIONS] as any}/><Filter value={value.recurrence} set={v=>onChange({...value,recurrence:v})} items={[["all","Toutes récurrences"],...PROGRAM_RECURRENCE_OPTIONS] as any}/><Filter value={value.importance} set={v=>onChange({...value,importance:v})} items={[["all","Toutes importances"],...PROGRAM_IMPORTANCE_OPTIONS] as any}/></div>}
function Filter({value,set,items}:{value:string;set:(v:string)=>void;items:[string,string][]}){return <Select value={value} onValueChange={set}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{items.map(([v,l])=><SelectItem key={v} value={v}>{l}</SelectItem>)}</SelectContent></Select>}
