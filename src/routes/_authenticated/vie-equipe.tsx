import{createFileRoute}from"@tanstack/react-router";import{useMutation,useQuery,useQueryClient}from"@tanstack/react-query";import{useState}from"react";import{Plus,Trash2}from"lucide-react";import{toast}from"sonner";import{AppShell,EmptyState}from"@/components/AppShell";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Dialog,DialogContent,DialogFooter,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Textarea}from"@/components/ui/textarea";import{Badge}from"@/components/ui/badge";import{useCurrentRole}from"@/hooks/useAuth";import{supabase}from"@/integrations/supabase/client";import{logAction,polesQuery}from"@/lib/icc";
export const Route=createFileRoute("/_authenticated/vie-equipe")({head:()=>({meta:[{title:"Vie d’équipe — COM ICC Le Mans"}]}),component:Page});const db=()=>supabase as any;const TYPES:any={detente:"Sortie / détente",restaurant:"Restaurant",atelier:"Atelier",parc:"Parc",repas_maison:"Repas à la maison",sport:"Activité sportive",anniversaire:"Anniversaire",attractions:"Attractions / jeux",autre:"Autre",a_definir:"Type à déterminer par sondage"};const RESP:any={yes:"Je participe ❤️",maybe:"Peut-être",no:"Je ne pourrai pas"};function perm(uid:string|undefined,a:string){return useQuery({queryKey:["ve-perm",uid,a],enabled:!!uid,queryFn:async()=>{const{data,error}=await db().rpc("access_scope_for_user",{p_user:uid,p_module:"vie_equipe",p_action:a});if(error)throw error;return data as string}})}function fmt(v:any){return v?new Date(v).toLocaleString("fr-FR",{dateStyle:"medium",timeStyle:"short"}):"À définir"}
function Page(){const{member,userId,isTechAdmin}=useCurrentRole(),qc=useQueryClient(),poles=useQuery(polesQuery),po=perm(userId,"organiser"),pv=perm(userId,"valider"),canO=isTechAdmin||po.data!=="interdit",canV=isTechAdmin||pv.data!=="interdit";const ev=useQuery({queryKey:["ve-events"],queryFn:async()=>{const{data,error}=await db().from("team_life_events").select("*").order("created_at",{ascending:false});if(error)throw error;return data??[]}}),polls=useQuery({queryKey:["ve-polls"],queryFn:async()=>{const{data,error}=await db().from("team_life_polls").select("*").order("sort_order");if(error)throw error;return data??[]}}),opts=useQuery({queryKey:["ve-options"],queryFn:async()=>{const{data,error}=await db().from("team_life_poll_options").select("*").order("sort_order");if(error)throw error;return data??[]}}),votes=useQuery({queryKey:["ve-votes"],queryFn:async()=>{const{data,error}=await db().from("team_life_poll_votes").select("*");if(error)throw error;return data??[]}}),parts=useQuery({queryKey:["ve-parts"],queryFn:async()=>{const{data,error}=await db().from("team_life_participations").select("*");if(error)throw error;return data??[]}});const refresh=()=>Promise.all(["ve-events","ve-polls","ve-options","ve-votes","ve-parts"].map(x=>qc.invalidateQueries({queryKey:[x]})));const[tab,setTab]=useState("future"),[detail,setDetail]=useState<any>(null),[open,setOpen]=useState(false),[title,setTitle]=useState(""),[desc,setDesc]=useState(""),[kind,setKind]=useState("detente"),[custom,setCustom]=useState(""),[links,setLinks]=useState(""),[aud,setAud]=useState("team"),[poleIds,setPoleIds]=useState<string[]>([]);const create=useMutation({mutationFn:async()=>{if(!title.trim())throw Error("Titre obligatoire");const idea_links=links.split("\n").map(x=>x.trim()).filter(Boolean).map(url=>({url}));const{error}=await db().from("team_life_events").insert({title:title.trim(),description:desc.trim()||null,event_kind:kind,custom_kind:kind==="autre"?custom.trim()||null:null,idea_links,audience_kind:aud,audience_pole_ids:aud==="poles"?poleIds:[],status:"proposal"});if(error)throw error},onSuccess:async()=>{setOpen(false);toast.success("Proposition publiée");await refresh()},onError:(e:any)=>toast.error(e.message)});const[pEvent,setPEvent]=useState<any>(null),[blocks,setBlocks]=useState([{q:"Quelle date ?",kind:"date",multi:true,options:""}]),[deadline,setDeadline]=useState("");function addBlock(){setBlocks(x=>[...x,{q:"",kind:"custom",multi:false,options:""}])}const createPoll=useMutation({mutationFn:async()=>{for(let i=0;i<blocks.length;i++){const b=blocks[i],labels=b.options.split("\n").map(x=>x.trim()).filter(Boolean);if(!b.q||labels.length<2)throw Error("Chaque bloc doit avoir une question et au moins 2 réponses");const{data:p,error}=await db().from("team_life_polls").insert({event_id:pEvent.id,question:b.q,block_kind:b.kind,allow_multiple:b.multi,closes_at:deadline?new Date(deadline).toISOString():null,status:"open",sort_order:i}).select("id").single();if(error)throw error;const{error:o}=await db().from("team_life_poll_options").insert(labels.map((label,j)=>({poll_id:p.id,label,sort_order:j})));if(o)throw o}await db().from("team_life_events").update({status:"polling"}).eq("id",pEvent.id)},onSuccess:async()=>{setPEvent(null);toast.success("Sondage publié");await refresh()},onError:(e:any)=>toast.error(e.message)});const vote=useMutation({mutationFn:async({p,o}:{p:any,o:any})=>{if(!member?.id)throw Error("Compte non relié");if(p.closes_at&&new Date(p.closes_at)<new Date())throw Error("Sondage terminé");if(!p.allow_multiple)await db().from("team_life_poll_votes").delete().eq("poll_id",p.id).eq("member_id",member.id);const mine=(votes.data??[]).find((v:any)=>v.poll_id===p.id&&v.option_id===o.id&&v.member_id===member.id);if(mine)await db().from("team_life_poll_votes").delete().eq("id",mine.id);else{const{error}=await db().from("team_life_poll_votes").insert({poll_id:p.id,option_id:o.id,member_id:member.id});if(error)throw error}},onSuccess:refresh});const[sched,setSched]=useState<any>(null),[start,setStart]=useState(""),[end,setEnd]=useState(""),[loc,setLoc]=useState(""),[budget,setBudget]=useState(""),[respDl,setRespDl]=useState("");const schedule=useMutation({mutationFn:async()=>{if(!start)throw Error("Date obligatoire");const{error}=await db().from("team_life_events").update({status:"scheduled",starts_at:new Date(start).toISOString(),ends_at:end?new Date(end).toISOString():null,location:loc||null,budget_planned:budget?Number(budget):null,response_deadline:respDl?new Date(respDl).toISOString():null,approved_by:userId,approved_at:new Date().toISOString()}).eq("id",sched.id);if(error)throw error},onSuccess:async()=>{setSched(null);toast.success("Communion fraternelle planifiée");await refresh()}});const respond=useMutation({mutationFn:async({id,r}:{id:string,r:string})=>{const e=(ev.data??[]).find((x:any)=>x.id===id);if(e?.response_deadline&&new Date(e.response_deadline)<new Date())throw Error("Date limite dépassée");const{error}=await db().from("team_life_participations").upsert({event_id:id,member_id:member!.id,response:r,responded_at:new Date().toISOString()},{onConflict:"event_id,member_id"});if(error)throw error},onSuccess:refresh,onError:(e:any)=>toast.error(e.message)});const trash=useMutation({mutationFn:async(id:string)=>{const{error}=await db().from("team_life_events").update({deleted_at:new Date().toISOString(),deleted_by:userId}).eq("id",id);if(error)throw error;await logAction({action:"vie_equipe_corbeille",entity:"team_life",entityId:id,actorName:member?.full_name})},onSuccess:refresh});const now=new Date(),all=(ev.data??[]).filter((x:any)=>!x.deleted_at),future=all.filter((x:any)=>x.status==="scheduled"&&(!x.ends_at||new Date(x.ends_at)>=now)),past=all.filter((x:any)=>x.status==="scheduled"&&x.ends_at&&new Date(x.ends_at)<now),proposals=all.filter((x:any)=>x.status==="proposal"||x.status==="polling"),shown=tab==="future"?future:tab==="proposals"?proposals:past;
return <AppShell title="Vie d’équipe" subtitle="Communions fraternelles : idées, sondages, organisation et souvenirs — hors statistiques opérationnelles." actions={<Button size="sm" onClick={()=>setOpen(true)}><Plus className="size-4"/>Faire une proposition</Button>}><div className="flex flex-wrap gap-2">{[["future",`À venir (${future.length})`],["proposals",`Propositions & sondages (${proposals.length})`],["past",`Passés (${past.length})`]].map(([k,l])=><Button key={k} size="sm" variant={tab===k?"default":"outline"} onClick={()=>setTab(k)}>{l}</Button>)}</div><div className="mt-4 space-y-3">{shown.length?shown.map((e:any)=><Card key={e.id} className="cursor-pointer hover:border-icc-violet/40" onClick={()=>setDetail(e)}><CardContent className="flex flex-wrap justify-between gap-3 p-4"><div><div className="flex gap-2"><Badge>{TYPES[e.event_kind]??e.custom_kind??e.event_kind}</Badge><Badge variant="outline">{e.status==="polling"?"Sondage en cours":e.status==="proposal"?"Proposition":e.status==="scheduled"?"Planifié":""}</Badge></div><b className="mt-2 block text-lg">{e.title}</b><p className="line-clamp-2 text-sm text-muted-foreground">{e.description}</p></div><div className="text-sm"><b>{fmt(e.starts_at)}</b>{e.location?<p>{e.location}</p>:null}</div></CardContent></Card>):<EmptyState title="Rien ici pour le moment"/>}</div>
<Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-xl"><DialogHeader><DialogTitle>Nouvelle proposition</DialogTitle></DialogHeader><F l="Titre"><Input value={title} onChange={e=>setTitle(e.target.value)}/></F><F l="Type de communion fraternelle"><select className="h-10 w-full rounded-md border bg-background px-3" value={kind} onChange={e=>setKind(e.target.value)}>{Object.entries(TYPES).map(([k,l]:any)=><option value={k} key={k}>{l}</option>)}</select></F>{kind==="autre"?<F l="Préciser"><Input value={custom} onChange={e=>setCustom(e.target.value)}/></F>:null}<F l="Description"><Textarea rows={5} value={desc} onChange={e=>setDesc(e.target.value)}/></F><F l="Liens / idées (un lien par ligne)"><Textarea value={links} onChange={e=>setLinks(e.target.value)} placeholder="https://..."/></F><F l="Audience"><select className="h-10 w-full rounded-md border bg-background px-3" value={aud} onChange={e=>setAud(e.target.value)}><option value="team">Toute l’équipe</option><option value="poles">Un ou plusieurs pôles</option></select></F>{aud==="poles"?(poles.data??[]).map((p:any)=><label key={p.id} className="mr-3 inline-flex gap-2 text-sm"><input type="checkbox" checked={poleIds.includes(p.id)} onChange={x=>setPoleIds(v=>x.target.checked?[...v,p.id]:v.filter(i=>i!==p.id))}/>{p.name}</label>):null}<DialogFooter><Button onClick={()=>create.mutate()}>Publier</Button></DialogFooter></DialogContent></Dialog>
<Dialog open={!!detail} onOpenChange={o=>!o&&setDetail(null)}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-2xl">{detail?<><DialogHeader><DialogTitle>{detail.title}</DialogTitle></DialogHeader><p>{detail.description}</p>{(detail.idea_links??[]).length?<div><b>Liens / idées</b>{detail.idea_links.map((x:any,i:number)=><a className="block text-sm text-icc-violet underline" href={x.url} target="_blank" rel="noreferrer" key={i}>{x.url}</a>)}</div>:null}{(polls.data??[]).filter((p:any)=>p.event_id===detail.id).map((p:any)=><div key={p.id} className="rounded-xl border p-3"><div className="flex justify-between"><b>{p.question}</b><small>{p.allow_multiple?"Plusieurs réponses possibles":"Une réponse"}{p.closes_at?` · jusqu’au ${fmt(p.closes_at)}`:""}</small></div><div className="mt-2 space-y-2">{(opts.data??[]).filter((o:any)=>o.poll_id===p.id).map((o:any)=>{const vs=(votes.data??[]).filter((v:any)=>v.option_id===o.id),mine=vs.some((v:any)=>v.member_id===member?.id);return <button className={`flex w-full justify-between rounded-lg border p-2 ${mine?"border-icc-violet bg-muted":""}`} onClick={()=>vote.mutate({p,o})} key={o.id}><span>{o.label}</span><b>{vs.length} vote(s)</b></button>})}</div></div>)}{detail.status==="scheduled"?<><div className="flex flex-wrap gap-2">{Object.entries(RESP).map(([r,l]:any)=><Button size="sm" variant="outline" key={r} onClick={()=>respond.mutate({id:detail.id,r})}>{l}</Button>)}</div>{detail.response_deadline?<small>Réponse souhaitée avant {fmt(detail.response_deadline)} · échéance prolongeable par l’organisation.</small>:null}</>:null}<div className="flex flex-wrap gap-2">{canO&&detail.status!=="scheduled"?<Button variant="outline" onClick={()=>{setPEvent(detail);setDetail(null)}}>Créer / compléter le sondage</Button>:null}{canV&&detail.status!=="scheduled"?<Button onClick={()=>{setSched(detail);setDetail(null)}}>Organiser / fixer la date</Button>:null}{canV&&tab==="past"?<Button variant="destructive" onClick={()=>trash.mutate(detail.id)}><Trash2 className="size-4"/>Mettre à la corbeille</Button>:null}</div></>:null}</DialogContent></Dialog>
<Dialog open={!!pEvent} onOpenChange={o=>!o&&setPEvent(null)}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>Construire le sondage</DialogTitle></DialogHeader>{blocks.map((b,i)=><div key={i} className="space-y-2 rounded-xl border p-3"><Input placeholder="Question" value={b.q} onChange={e=>setBlocks(x=>x.map((z,j)=>j===i?{...z,q:e.target.value}:z))}/><select className="h-10 w-full rounded-md border px-3" value={b.kind} onChange={e=>setBlocks(x=>x.map((z,j)=>j===i?{...z,kind:e.target.value}:z))}><option value="date">Date</option><option value="time">Créneau horaire</option><option value="type">Type</option><option value="location">Lieu</option><option value="budget">Budget / personne</option><option value="custom">Autre question</option></select><Textarea placeholder="Une réponse possible par ligne" value={b.options} onChange={e=>setBlocks(x=>x.map((z,j)=>j===i?{...z,options:e.target.value}:z))}/><label className="flex gap-2 text-sm"><input type="checkbox" checked={b.multi} onChange={e=>setBlocks(x=>x.map((z,j)=>j===i?{...z,multi:e.target.checked}:z))}/>Autoriser plusieurs réponses dans ce bloc</label></div>)}<Button variant="outline" onClick={addBlock}>+ Ajouter un bloc</Button><F l="Date limite de réponse"><Input type="datetime-local" value={deadline} onChange={e=>setDeadline(e.target.value)}/></F><DialogFooter><Button onClick={()=>createPoll.mutate()}>Publier le sondage</Button></DialogFooter></DialogContent></Dialog>
<Dialog open={!!sched} onOpenChange={o=>!o&&setSched(null)}><DialogContent className="max-h-[90vh] overflow-y-auto"><DialogHeader><DialogTitle>Organiser la communion fraternelle</DialogTitle></DialogHeader><F l="Début"><Input type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/></F><F l="Fin"><Input type="datetime-local" value={end} onChange={e=>setEnd(e.target.value)}/></F><F l="Lieu"><Input value={loc} onChange={e=>setLoc(e.target.value)}/></F><F l="Budget prévisionnel"><Input type="number" value={budget} onChange={e=>setBudget(e.target.value)}/></F><F l="Date limite de réponse (prolongeable)"><Input type="datetime-local" value={respDl} onChange={e=>setRespDl(e.target.value)}/></F><DialogFooter><Button onClick={()=>schedule.mutate()}>Valider et planifier</Button></DialogFooter></DialogContent></Dialog></AppShell>}
function F({l,children}:any){return <div className="space-y-1"><Label>{l}</Label>{children}</div>}
