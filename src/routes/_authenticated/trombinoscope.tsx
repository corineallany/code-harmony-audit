import { createFileRoute, Link } from "@tanstack/react-router";import{useQuery}from"@tanstack/react-query";import{useMemo,useState}from"react";import{AppShell,EmptyState}from"@/components/AppShell";import{cropStyle}from"@/components/ImageCropper";import{useCurrentRole}from"@/hooks/useAuth";import{membersQuery,polesQuery,settingsQuery}from"@/lib/icc";import{Avatar,AvatarFallback,AvatarImage}from"@/components/ui/avatar";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Card,CardContent}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{Skeleton}from"@/components/ui/skeleton";
export const Route=createFileRoute("/_authenticated/trombinoscope")({head:()=>({meta:[{title:"Trombinoscope — COM ICC Le Mans"}]}),component:Trombinoscope});const MONTHS=["Janvier","Février","Mars","Avril","Mai","Juin","Juillet","Août","Septembre","Octobre","Novembre","Décembre"];function initials(n:string){return n.split(" ").filter(Boolean).slice(0,2).map(p=>p[0]?.toUpperCase()).join("")}function memberState(m:any){if(m.status==="archived")return"archived";if(m.status==="inactive")return"inactive";if(m.status==="active"&&m.training_start&&!m.training_done)return"training";return"active"}function stateLabel(m:any){const s=memberState(m);return s==="training"?"En formation":s==="inactive"?"Inactif":s==="archived"?"Archivé":"Actif"}function birthdayLabel(m:any){return m.birthday_day&&m.birthday_month?`${m.birthday_day} ${MONTHS[m.birthday_month-1]?.toLowerCase()??""}`:null}function daysUntilBirthday(m:any){if(!m.birthday_day||!m.birthday_month)return 999;const n=new Date(),t=new Date(n.getFullYear(),n.getMonth(),n.getDate());let x=new Date(n.getFullYear(),m.birthday_month-1,m.birthday_day);if(x<t)x=new Date(n.getFullYear()+1,m.birthday_month-1,m.birthday_day);return Math.round((x.getTime()-t.getTime())/86400000)}
function Trombinoscope(){const members=useQuery(membersQuery),poles=useQuery(polesQuery),settings=useQuery(settingsQuery),{isAdmin}=useCurrentRole(),[search,setSearch]=useState(""),[status,setStatus]=useState("all"),[role,setRole]=useState("all"),[pole,setPole]=useState("all"),[sort,setSort]=useState("name");const poleName=useMemo(()=>new Map((poles.data??[]).map(p=>[p.id,p.name])),[poles.data]),activePoles=useMemo(()=>(poles.data??[]).filter(p=>!p.archived),[poles.data]),directionLabels=useMemo(()=>{const s=settings.data as any,l=new Map<string,string>();if(!s)return l;const st=s.direction_structure??"responsable_adjoint";if(s.supervisor_member_id)l.set(s.supervisor_member_id,st==="responsable_groupes"?"Responsable général":"Responsable");if(st==="responsable_adjoint"&&s.adjoint_member_id)l.set(s.adjoint_member_id,"Adjoint");return l},[settings.data]);const rows=useMemo(()=>{const links=members.data?.links??[],needle=search.trim().toLowerCase(),list=(members.data?.members??[]).map((raw:any)=>({...raw,poles:links.filter(l=>l.member_id===raw.id).map(l=>({id:l.pole_id,name:poleName.get(l.pole_id)??"Pôle",referent:l.is_referent})),directionLabel:directionLabels.get(raw.id)??null})).filter((m:any)=>{if(m.status==="archived")return status==="archived";return(!needle||[m.full_name,m.login_email,m.affiliations,m.directionLabel].some(v=>v?.toLowerCase().includes(needle)))&&(status==="all"||memberState(m)===status)&&(role==="all"||(role==="referent"?m.poles.some((p:any)=>p.referent):!m.poles.some((p:any)=>p.referent)))&&(pole==="all"||m.poles.some((p:any)=>p.id===pole))});list.sort((a:any,b:any)=>sort==="arrival-new"?((b.arrival_year??0)*12+(b.arrival_month??0))-((a.arrival_year??0)*12+(a.arrival_month??0)):sort==="arrival-old"?((a.arrival_year??9999)*12+(a.arrival_month??12))-((b.arrival_year??9999)*12+(b.arrival_month??12)):sort==="birthday"?daysUntilBirthday(a)-daysUntilBirthday(b):a.full_name.localeCompare(b.full_name,"fr"));return list},[members.data,poleName,directionLabels,search,status,role,pole,sort]);const upcoming=useMemo(()=>rows.filter((m:any)=>m.status!=="archived"&&daysUntilBirthday(m)<=7),[rows]);return <AppShell title="Trombinoscope" subtitle={`${rows.length} membre(s)`} actions={isAdmin?<Button asChild size="sm"><Link to="/membre/$id" params={{id:"nouveau"}}>+ Ajouter un membre</Link></Button>:undefined}>{upcoming.length?<div className="mb-4 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm"><b>🎂 Anniversaire à venir</b><p className="mt-1 text-muted-foreground">{upcoming.map((m:any)=>`${m.full_name} · ${birthdayLabel(m)}${daysUntilBirthday(m)===0?" · aujourd’hui":` · J-${daysUntilBirthday(m)}`}`).join(" • ")}</p></div>:null}<div className="mb-5 grid gap-2 md:grid-cols-2 xl:grid-cols-5"><Input placeholder="Rechercher un membre…" value={search} onChange={e=>setSearch(e.target.value)}/><Filter value={status} onChange={setStatus} items={[["all","Tous les statuts"],["active","Actif"],["training","En formation"],["inactive","Inactif"],["archived","Archivé"]]}/><Filter value={role} onChange={setRole} items={[["all","Tous les rôles"],["referent","Référent"],["equipier","Équipier"]]}/><Filter value={pole} onChange={setPole} items={[["all","Tous les pôles"],...activePoles.map(p=>[p.id,p.name])]}/><Filter value={sort} onChange={setSort} items={[["name","Nom A–Z"],["arrival-new","Intégration récente"],["arrival-old","Ancienneté"],["birthday","Anniversaire proche"]]}/></div>{members.isLoading||settings.isLoading?<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[0,1,2,3,4,5].map(i=><Skeleton key={i} className="h-44 rounded-xl"/>)}</div>:rows.length===0?<EmptyState title="Aucun membre trouvé"/>:<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{rows.map((m:any)=>{const state=memberState(m),arrival=m.arrival_year?`${m.arrival_month?`${MONTHS[m.arrival_month-1]} `:""}${m.arrival_year}`:null,refs=m.poles.filter((p:any)=>p.referent);return <Link key={m.id} to="/membre/$id" params={{id:m.id}} className="block"><Card className={`h-full transition hover:border-icc-violet/50 hover:shadow-sm ${state==="archived"?"opacity-50":state==="inactive"?"opacity-70":""}`}><CardContent className="p-4"><div className="flex gap-3"><Avatar className="size-16 shrink-0">{m.photo_url?<AvatarImage src={m.photo_url} alt={m.full_name} className="object-cover" style={cropStyle(m.photo_crop)}/>:null}<AvatarFallback>{initials(m.full_name)}</AvatarFallback></Avatar><div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-2"><p className="truncate font-black text-icc-violet">{m.full_name}</p><Badge variant={state==="active"?"default":"secondary"}>{stateLabel(m)}</Badge></div><div className="mt-2 flex flex-wrap items-center gap-1">{m.is_icc?<Badge variant="outline">✨ ICC</Badge>:null}{m.is_ejp?<Badge variant="outline">⭐ EJP</Badge>:null}{m.directionLabel?<Badge variant="outline">{m.directionLabel}</Badge>:null}{refs.length?<><Badge variant="outline">🏅 Référent</Badge><strong className="text-xs">{refs.map((p:any)=>p.name).join(" · ")}</strong></>:null}</div></div></div><div className="mt-3 space-y-1 text-xs text-muted-foreground">{arrival?<p>📍 Intégration : {arrival}</p>:null}{birthdayLabel(m)?<p>🎂 Anniversaire : {birthdayLabel(m)}</p>:null}{state==="training"?<p>🎓 Formation : {m.training_start?new Date(m.training_start).toLocaleDateString("fr-FR"):"en cours"}</p>:null}</div><div className="mt-3 flex flex-wrap gap-1">{m.poles.length===0?<span className="text-xs text-muted-foreground">Sans pôle</span>:m.poles.map((p:any)=><Badge key={p.id} variant="secondary" className="text-[0.7rem]">{p.name}</Badge>)}</div></CardContent></Card></Link>})}</div>}</AppShell>}
function Filter({value,onChange,items}:{value:string;onChange:(v:string)=>void;items:string[][]}){return <Select value={value} onValueChange={onChange}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{items.map(([v,l])=><SelectItem key={v} value={v}>{l}</SelectItem>)}</SelectContent></Select>}
